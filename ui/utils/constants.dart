import 'package:flutter/material.dart';

const double kPadding = 14;
const double kBorderRadius = kPadding;

const EdgeInsets kAllPadding = const EdgeInsets.all(kPadding);
const BorderRadius kAllBorderRadius =
    const BorderRadius.all(Radius.circular(kBorderRadius));
